{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx prisma@6.19.3 migrate resolve --rolled-back 20260528000000_add_variable_map; npx prisma@6.19.3 migrate deploy; node .next/standalone/server.js",
    "restartPolicyType": "ON_FAILURE"
  },
  "crons": [
    {
      "schedule": "0 8 * * *",
      "command": "curl -s -o /dev/null -w '%{http_code}' -H 'Authorization: Bearer $CRON_SECRET' $NEXT_PUBLIC_BASE_URL/api/cron/reminders"
    }
  ]
}
